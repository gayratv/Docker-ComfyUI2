
cd /mnt/f/_prg/python/Docker-ComfyUI/docker-base/docker-cuda-test
export IMAGE_NAME="cuda-test"
export VERSION="V1"


nvidia-smi -L

docker run -it --privileged --gpus all -p 8188:8188 -p 1188:1188 \
--env-file .env \
-e MODELS=$MODELS \
--name "$IMAGE_NAME" "$IMAGE_NAME:$VERSION" \
tmux-s-bash.sh

--runtime=nvidia

sudo mkdir -p ~/.docker/cli-plugins/docker-buildx
sudo rm -rf ~/.docker/cli-plugins/docker-buildx
sudo curl -L \
  https://github.com/docker/buildx/releases/download/v0.26.0-rc1/buildx-v0.26.0-rc1.linux-amd64 \
  -o ~/.docker/cli-plugins/docker-buildx
chmod +x ~/.docker/cli-plugins/docker-buildx

docker pull gayrat/cuda-test:V1

docker run -it gayrat/cuda-test:V1
docker run -it --gpus all gayrat/cuda-test:V1
docker run -it --runtime nvidia gayrat/cuda-test:V1
docker run -it --runtime=nvidia gayrat/cuda-test:V1



mkdir /workspace
cd /workspace

sudo apt update
sudo apt install -y software-properties-common

sudo add-apt-repository -y ppa:deadsnakes/ppa
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3.11-distutils

python3.11 --version

python3.11 -m venv .venv
source .venv/bin/activate


git clone https://github.com/thu-ml/SageAttention.git
cd SageAttention

python3 -m pip install --upgrade pip

  python3 -m pip install --cache-dir=/root/pip-cache packaging

python3 -m pip install --cache-dir=/root/pip-cache  torch==2.7.0 torchaudio==2.7.0 \
    --extra-index-url https://download.pytorch.org/whl/cu126

python3 -m pip install --cache-dir=/root/pip-cache numpy

export TORCH_CUDA_ARCH_LIST="8.6"
export TORCH_CUDA_ARCH_LIST="8.6;8.9"
export EXT_PARALLEL=4
export NVCC_APPEND_FLAGS="--threads 8"
export MAX_JOBS=32
export FORCE_CUDA=1

sudo apt-get update
sudo apt-get install python3.11-dev
pip install --upgrade pip setuptools wheel build



python3 setup.py install


